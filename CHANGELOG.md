# Changelog

All notable changes to `scitex-compat` are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);
versions follow [Semantic Versioning](https://semver.org/).

## [Unreleased]

- docs: refresh README Architecture tree and skill docs to match actual source layout (`_compat.py` vs `_deprecated.py`/`_shims/`) and correct `@deprecated` signature (`new_name`, `removal_version`).

## [0.1.5]

- Initial CHANGELOG entry — see git log for prior history.
